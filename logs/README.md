# Experiment logs

Each run writes `architecture.json`, `metrics.json`, and `train.log` beneath
`logs/<experiment-name>/`. Experiment artifacts are intentionally not tracked
by Git.

Logs produced before the repository reorganization are preserved locally in
`logs/archive/`.
